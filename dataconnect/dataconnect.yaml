specVersion: "v1beta"
serviceId: "alerta-vecinal"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "alerta-vecinal-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
